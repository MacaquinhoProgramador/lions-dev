let { baralhos } = require('./data');

function criarFlashcard(flashcards, { id: id, perguntas: perguntas, resposta: resposta, idBaralho: idBaralho }){
    baralhos[index].push(flashcards);

}

module.exports = { criarFlashcard }